# NEXT — с чего начать новую сессию

Читать этот файл вместо истории. Обновлять в конце каждой сессии (≤ 40 строк).

## Как работаем
- Ветка `feature/vocab-learning-engine`, выкладка — fast-forward в `master` (деплой на Pages + Supabase `ixtfifglohppaimvyvui`).
- Одна задача — одна сессия. Агенты — только для вёрстки по готовой спеке; дизайнер — только gate B по 1–2 скриншотам.
- Читать точечно (grep, диапазоны строк). Спеки #65 длиннее лимита 150 строк — перед вёрсткой ужать.
- Тесты модели: `cd supabase/functions && DENO_NO_PACKAGE_JSON=1 deno test _shared/learning/` (75). 4 старых теста в `_shared/*_test.ts` падают без сети/ключей — не наши.
- Коммиты — с двумя строками атрибуции (Co-Authored-By, Claude-Session).

## Готово (сервер механики слов)
- #58–#61: таблицы, модель памяти, `review-submit` (подписанные билеты, одна транзакция `learning_apply_review`).
- #62: дистракторы (`learning_distractor_pool` + `pickOptions`). Открыт только под кэш ИИ «путают с».
- #63: `buildSession` (Сегодня / папка: повторить · раунд 7 · практика), `session-build` (preview/start),
  контракт `packages/shared/schemas/study.ts`, результат ответа по спеке (retry, «Уже знаю», карточка пары).
  **Не проверено вживую** — нет клиента. Первый сквозной прогон — вместе с #68.
- #40 настройки, выход, удаление аккаунта — на проде.

## Дальше по порядку
1. **#65 P2 — токены и примитивы.** Черновик агента в ветке `p2-tokens` (остановлен лимитом, не проверен):
   прогнать tsc/eslint/jest, доделать, слить в рабочую ветку.
2. **#66** навигация: «Словарь» первым, блок «Сегодня».
3. **#67** экран упражнений (R1 R2 P1 P2 W1 W2 intro pair_card) на моках по `study.ts`.
4. **#68** «Сегодня» + окно «Повторим?» → первый сквозной прогон с `session-build` / `review-submit`.
5. **#69** раунд папки, **#70** карта папки + **#85** очередь, **#71** пары.
6. Позже: #64 контексты (откроет C1/C2 и «Использую»), #73 learning-overview, #74 знаки, словарь 2.0 #75–#84.

## Известные ограничения
- Навык «Использую» и форматы C* не выдаются до #64.
- Разбор ошибки — шаблонные строки; различие в карточке пары — `null` до #71/#74.
- Поиск партнёра путаницы по пиньиню — 1000 последних слов словаря.
