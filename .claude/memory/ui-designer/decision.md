# Decision Memory — ui-designer

Локальные дизайн-решения агента и их причины — то, что не поднялось до уровня
TZ.md §12, но должно оставаться стабильным между задачами.

Правило: решение записывается вместе с источником (референс, документ, ответ
пользователя). Решение без источника — догадка, и в память не идёт
(design-system-agent, Boundaries: «не выдумывать цвета из головы»).
